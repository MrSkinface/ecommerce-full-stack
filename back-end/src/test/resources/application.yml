spring:
  datasource:
    url: jdbc:tc:postgresql:15:///full-stack-ecommerce
    username: postgres
    password: postgres
  liquibase:
    change-log: classpath:liquibase/changelog-master.xml

stripe:
  key:
    secret: secret